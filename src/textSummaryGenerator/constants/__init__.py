from pathlib import Path

# Use this to define the paths of the required files and directiories. 
# This will help us to avoid hardcoding the paths in the code and make it more flexible.

CONFIG_FILE_PATH = Path('config/config.yaml')
PARAMS_FILE_PATH = Path('params.yaml')